ZDOTDIR=$HOME/.config/zsh
skip_global_compinit=1

# use this to detect compinit calls from elsewhere
# autoload -Uz +X compinit
# functions[compinit]=$'print -u2 \'compinit being called at \'${funcfiletrace[1]}
# '${functions[compinit]}

